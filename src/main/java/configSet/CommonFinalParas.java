package configSet;

import com.fasterxml.jackson.annotation.JsonProperty;

import java.util.List;

/**
 * @author Benchen Ye
 * @create 2024-09--19:45
 * @function
 */
/*
public class CommonFinalParas {
    // MandatoryParameters
    public static final String bam_file;
    public static final String genome;
    public static final String output_name;
    public static final String region_plot;

    // CommonParameters
    public static final int num_datapoints;
    public static final int middle_datapoints;
    public static final int flanking_region_datapoints;
    public static final String[] plot_title;

    // CalculatorParaters
    public static final String database;

    public static final int flanking_size;

    public static final int flank_factor;
    public static final int robust;
    public static final int samprate;
    public static final int cores_num;

    public static final String scaler_method;
    public static final int min_mapq;
    public static final int frag_len;
    public static final int buf_size;

    public static final String strand_spec;
    public static final int fi_tag;
    public static final String[] gene_list;
    public static final boolean rnaseq_gb;
    public static final int large_interval;
    public static final String interval_type;
    public static final String[] region_labels;


    static {
        GetConfigParaValue config_obj = new GetConfigParaValue();
        // MandatoryParameters
        bam_file = config_obj.getBamFile();
        genome = config_obj.getGenome();
        output_name = config_obj.getOutputName();
        region_plot = config_obj.getRegionPlot();

        // CommonParameters
        num_datapoints = config_obj.getNumDatapoints();
        middle_datapoints = config_obj.getMiddleDatapoints();
        flanking_region_datapoints = config_obj.getFlankingRegionDatapoints();
        plot_title = config_obj.getPlotTitle();

        // CalculatorParaters
        database = config_obj.getDatabase();

        flanking_size = config_obj.getFlankingSize();

        flank_factor = config_obj.getFlankFactor();
        robust = config_obj.getRobust();
        samprate = config_obj.getSamprate();
        cores_num = config_obj.getCoresNum();

        scaler_method = config_obj.getScalerMethod();
        min_mapq = config_obj.getMinMapq();
        frag_len = config_obj.getFragLen();
        buf_size = config_obj.getBufSize();

        strand_spec = config_obj.getStrandSpec();
        fi_tag = config_obj.getFiTag();
        gene_list = config_obj.getGeneList();
        rnaseq_gb = config_obj.getRnaseqGb();
        large_interval = config_obj.getLargeInterval();
        interval_type = config_obj.getIntervalType();
        region_labels = config_obj.getRegionLabels();
    }
}
*/
