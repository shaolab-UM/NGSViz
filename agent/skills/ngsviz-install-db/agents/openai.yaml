interface:
  display_name: "ngsViz Reference Database Installer"
  short_description: "Install a validated pre-built ngsViz reference database"
  default_prompt: "Use $ngsviz-install-db to install the required pre-built genome annotation database."
