interface:
  display_name: "ngsViz Agent"
  short_description: "Safely orchestrate ngsViz computation and plotting"
  default_prompt: "Use $ngsviz-agent to preflight, validate, and orchestrate an ngsViz compute or visualization request."
