interface:
  display_name: "ngsViz Environment Setup"
  short_description: "Inspect and repair the ngsViz runtime configuration"
  default_prompt: "Use $ngsviz-setup to inspect and repair the ngsViz runtime configuration before validation."
