interface:
  display_name: "ngsViz GTF Database Builder"
  short_description: "Build and import an ngsViz database from UCSC GTF"
  default_prompt: "Use $ngsviz-build-db to build and import an ngsViz reference database from a UCSC GTF file."
