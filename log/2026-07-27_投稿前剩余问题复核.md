# 投稿前剩余问题复核日志

- 日期：2026-07-27
- 范围：当前 `HEAD`（`6561b9e`）Java 计算后端、相关测试、历史审计和近期修复提交。
- 模式：只读复核；未修改源码、测试或科学行为。

## 执行内容

- 盘点仓库结构、测试入口、历史审计和最近 Git 提交。
- 尝试调用本地 Kimi Code CLI 执行辅助全仓检索；因本计费周期额度耗尽，未获得候选问题输出，未将其未完成推理用作证据。
- 执行 `mvn test`，15 个测试中 1 个失败。
- 使用临时 Java 探针验证链特异性过滤覆盖 duplicate/MAPQ 结果。
- 复核 Center、CIGAR、secondary/supplementary、染色体右边界和 CPM 分母路径。

## 结果

- 新确认：
  - 链特异性判断会让低 MAPQ 或 duplicate reads 重新通过；
  - 多样本 Input/IgG 状态会泄漏到后续无背景样本。
- 当前仍存在：
  - Center 模式结果被普通 scaling 覆盖；
  - 覆盖度忽略 CIGAR；
  - secondary/supplementary alignments 未过滤；
  - 染色体右边界未裁剪；
  - CPM 分子分母过滤集合不一致。
- 发布阻断：
  - `QueryGenomeRangeTest.testQueryStartAtGeneStart` 失败。

## 输出

- `audit/07_submission_readiness_review_2026-07-27.md`

## Kimi Code 恢复复核

- 用户恢复额度并再次授权后，Kimi Code 完成当前 HEAD 的只读独立复核。
- Kimi 支持原报告全部 8 项判断。
- 经主审查员复核后新增：
  - exon 模型跨 transcript 共享并覆盖坐标列表；
  - exon coverage 闭区间错误和零填充；
  - broad-interval 尾侧首 bin 重复、末 bin 丢失；
  - Shiny 默认 scale ratio 绕过 CPM。
- 排除或降级：
  - `processJSON.R` 四元素向量不是直接 Java 命令；
  - `chrUn_*` 会通过 `startsWith("chr")` 检查；
  - `queryContained` 仅存在于仓库未调用的旧 API。
- 使用临时 Java 探针确认：
  - exon 列表复用后旧 transcript 坐标被下一 transcript 改写；
  - broad-interval 输出保留边界混合点，但重复 `tail[0]` 并丢弃 `tail[20]`。
