# BAM 索引预检修复

- 在 `BAMAttribute` 开始顺序扫描前验证 BAM 是否具有 htsjdk 可识别的 BAI 或 CSI 索引。
- 验证 BAM 头部排序方式为 `coordinate`。
- 主动加载索引，提前报告损坏或不兼容的索引。
- 错误信息包含 BAM 绝对路径和 `samtools index` 修复提示。
- README 同步说明 BAM 必须按坐标排序并具有可读的 BAI 或 CSI 索引。
- 新增回归测试，覆盖缺失索引、非坐标排序、损坏索引和有效索引。

## 验证

- `mvn -q -Dtest=BAMAttributeIndexValidationTest test`：通过，4 项测试全部通过。
- `mvn test`：共 15 项测试，14 项通过，1 项失败。
- 失败项：`QueryGenomeRangeTest.testQueryStartAtGeneStart`，期望值为 85，实际值为 80；该失败在本次修复前已存在，与 BAM 索引预检无关。
- `git diff --check`：通过。
